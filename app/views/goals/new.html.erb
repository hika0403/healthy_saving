<%= render "shared/header" %>

<h1>目標設定</h1>
<%= form_with(model: @goal, url: goals_path, method: :post, local: true) do |goal| %>
  <%# <% if goal.errors.any? %>
    <%# <div id="error_explanation"> %>
      <%# <h2><%= pluralize(goal.errors.count, "error") %> 
      <%# prohibited this goal from being saved:</h2> %> 

      <%# <ul>
        <% goal.errors.full_messages.each do |message| %>
          <%# <li><%= message</li>  %>
        <%# <% end %>
      <%# </ul>  %>
    <%# </div> %>
  <%# <% end %>

  <div class="field">
    <%= goal.label :saving_goal %>
    <%= goal.text_field :saving_goal %>
  </div>

    <div class="field">
    <%= goal.label :weight_goal %>
    <%= goal.text_field :weight_goal %>
  </div>

  <div class="field">
    <label class="form-text">生年月日</label>
    <span class="indispensable">必須</span>
  </div>
  <div class='input-birth-wrap'>
    <%= raw sprintf(
      goal.date_select(
      :period,
      class:'select-birth',
      id:"birth-date",
      use_month_numbers: true,
      prompt:'--',
      start_year: 1930,
      end_year: (Time.now.year - 5),
      date_separator: '%s'),
      "<p> 年 </p>", "<p> 月 </p>") + "<p> 日 </p>" %>
  </div>

  <div class="field">
    <%= goal.label :purpose %>
    <%= goal.text_field :purpose %>
  </div>

  <div class="actions">
    <%= goal.submit %>
  </div>
<% end %>
