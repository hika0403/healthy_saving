    <div class="border col-7 container my-5">
        <br>
        <h2>Setting Goal</h2>
        <br>
        <%= form_with(model: @goal, url: goals_path, method: :post, local: true) do |goal| %>
        <div class="row align-items-center justify-content-center my-3">
            <div class="col-mdd-flex">
                <form>
                    <div class="form-group">
                      <%= goal.label :貯金金額 %>
                      <%= goal.text_field :saving_goal, class:"form-control", placeholder:"30" %>
                    </div>
                    <div class="form-group">
                      <%= goal.label :体重 %>
                      <%= goal.text_field :weight_goal, class:"form-control", placeholder:"50" %>
                    </div>
                    <div class="form-group">
                        <label>期限</label>
                        <span>（いつまでに達成するのか）</span>
                    </div>
                    <div>
                      <%= raw sprintf(
                        goal.date_select(
                          :period,
                          class:'form-control',
                          use_month_numbers: true,
                          prompt:'--',
                          start_year: Time.now.year,
                          end_year: (Time.now.year + 10),
                          date_separator: '%s'),
                        "<p> 年 </p>", "<p> 月 </p>") + "<p> 日 </p>" %>
                    </div>
                    <div class="form-group">
                      <%= goal.label :目的 %>
                      <%= goal.text_field :purpose, class:"form-control", placeholder:"カバンを買う" %>
                    </div>
                    <div class="container">
                      <%= goal.submit 'Create', class:"btn btn-outline-success btn-lg w-100" %>
                    </div>
                </form>
            </div>
        </div>
        <% end %>
    </div>
